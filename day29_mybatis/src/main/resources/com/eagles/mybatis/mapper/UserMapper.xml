<?xml version="1.0" encoding="UTF-8" ?>
<!--开发文档2.1.5节中可复制-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eagles.mybatis.mapper.UserMapper">
    <select id="getUserList" resultType="user">
        select * from tb_user
    </select>

<!--    <insert id="insertUserGetId" parameterType="com.eagles.mybatis.pojo.User" keyColumn="id" keyProperty="id" useGeneratedKeys="true">
        insert into tb_user(username,address)values(#{username},#{address})
    </insert>-->

</mapper>
